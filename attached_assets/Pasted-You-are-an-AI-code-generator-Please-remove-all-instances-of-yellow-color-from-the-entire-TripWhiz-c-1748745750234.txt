You are an AI code generator. Please remove all instances of yellow color from the entire TripWhiz codebase (React/Tailwind) and replace them with a neutral gray (e.g., `gray-500`) or fully remove them where appropriate. Your task is to update Tailwind CSS classes, inline styles, and any hex codes referencing yellow. Specifically:

1. **Tailwind Class Replacement**  
   - Search for any Tailwind utility classes containing `yellow-` (e.g., `bg-yellow-200`, `text-yellow-500`, `hover:bg-yellow-300`, etc.).  
   - Replace each with the equivalent gray shade:  
     - `yellow-50` → `gray-50`  
     - `yellow-100` → `gray-100`  
     - `yellow-200` → `gray-200`  
     - `yellow-300` → `gray-300`  
     - `yellow-400` → `gray-400`  
     - `yellow-500` → `gray-500`  
     - `yellow-600` → `gray-600`  
     - `yellow-700` → `gray-700`  
     - `yellow-800` → `gray-800`  
     - `yellow-900` → `gray-900`

2. **Inline Styles & Hex Codes**  
   - Search for any inline `style={{ color: '#XXXXXX' }}` or `style="color: #XXXXXX"` or CSS files where a yellow hex code appears (for example `#FFFF00`, `#FFEB3B`, `#FFC107`, etc.).  
   - Replace those hex codes with `#6B7280` (Tailwind’s `gray-500`) or another neutral gray of your choice.

3. **SVGs & Icons**  
   - If any SVG or icon uses a `fill="#FEEF08"` or similar yellow attribute, replace it with a neutral gray fill (`#6B7280`).

4. **CSS/SCSS Files**  
   - If your project contains any standalone CSS or SCSS files referencing yellow (e.g., `.highlight { background-color: yellow; }`), remove or replace those rules with gray equivalents (e.g., `.highlight { background-color: #6B7280; }`).

5. **Ensure Consistency**  
   - After replacing all yellow utilities and hex codes, verify there are no remaining instances of “yellow” in any `.js`, `.jsx`, `.ts`, `.tsx`, `.css`, `.scss`, or `.html` files.  
   - Remove any unused imports that referenced Tailwind’s yellow palette (if present).

6. **Examples**  
   - Change: `<button className="bg-yellow-500 hover:bg-yellow-600 text-white">`  
     To: `<button className="bg-gray-500 hover:bg-gray-600 text-white">`  
   - Change: `<div style={{ backgroundColor: '#FFEB3B' }}>`  
     To: `<div style={{ backgroundColor: '#6B7280' }}>`

Produce a summary of all files updated and the exact code changes made so that no yellow color remains in the site.  
